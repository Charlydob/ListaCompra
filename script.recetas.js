// ============ RECETAS (stub) ============
// Si no usas recetas aún, este archivo evita 404/MIME errors.
// Puedes borrarlo cuando implementes recetas de verdad.
document.addEventListener("DOMContentLoaded", () => {
  const tabRec = document.getElementById("tab-recetas");
  const vistaRec = document.getElementById("vista-recetas");
  if (!tabRec || !vistaRec) return;
  // placeholder: nada.
});
